[workspace]
channels = ["conda-forge"]
name = "chep-2026-proceedings-hep-packaging-coordination"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]

[feature.lint.dependencies]
prek = ">=0.5.3,<0.6"

[feature.lint.tasks]
lint = "prek run --all-files"

[environments]
lint = ["lint"]
